// Adds custom matchers like toBeInTheDocument(), toHaveTextContent(), etc.
// to every test file automatically — react-scripts runs this before each
// test suite.
import '@testing-library/jest-dom';
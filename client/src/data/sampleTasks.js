// Shared constants used across TaskForm, TaskItem, Sidebar, and Filters.
// (The original sample seed-data array that used to live here was removed
// once Stage 7 wired up the real API — it's no longer needed.)

export const CATEGORIES = ['Work', 'Personal', 'Study', 'Health', 'Other'];
export const PRIORITIES = ['low', 'medium', 'high'];
export const STATUSES = ['pending', 'in-progress', 'completed'];